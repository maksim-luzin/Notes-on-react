enum Routes {
  NewNote = '/new',
  ViewNote = '/view',
  EditNote = '/edit',
  ConfirmDelete = '/delete',
  Active = '/active',
  Archived = '/archived',
  ArchiveNote = '/archive',
  UnzipNote = '/unzip',
  All = '*',
  FirstStart = '/',
  NotFound = '/not-found'
}

export { Routes };
